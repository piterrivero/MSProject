server:
  port: 8081
spring:
  cloud: 
    config:
      server:
        git:
          default-label: configserver
          uri: https://github.com/piterrivero/MSProject
          search-paths: config-data
  application:
    name: config-service
